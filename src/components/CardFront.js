import React from 'react';

const CardFront = (id) => {

    return (
      <div background="#ffffff" className="memoryCard front">
        <p>{this.id}</p>
      </div>
    );
}

export default CardFront;
